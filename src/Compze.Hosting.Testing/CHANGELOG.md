# Changelog

## 0.1.0-alpha.1

- Initial release: the paradigm-neutral `TestingEndpointHost` with its `ITestingEndpointHostFeature` seam, and the pluggable-component test wiring (DI containers, serializers, database pools) extracted from `Compze.Tessaging.Hosting.Testing`.
