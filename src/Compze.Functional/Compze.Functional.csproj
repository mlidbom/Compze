<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <Nullable>enable</Nullable>
    <Version>0.8.0-beta.1</Version>
    <Description>Code that reads the way you would describe the algorithm in words, not code that must be read inside out. Powered by the pipe forward operator.</Description>
    <PackageTags>compze;functional;pipe;unit;extensions;csharp</PackageTags>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="NsDepCop" Version="2.6.0">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
  </ItemGroup>
</Project>