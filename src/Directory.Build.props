<Project>
	<!-- Enable package validation for all projects -->
	<Import Project="msbuild\EnablePackageValidation.props" />
	
	<!-- Ensure test configuration file exists before build -->
	<Import Project="msbuild\EnsurePluggableComponentsConfigExists.props" />

	<!-- Enable latest analyzer rules for all projects -->
	<PropertyGroup>
		<AnalysisLevel>latest-all</AnalysisLevel>
		<RunAnalyzersDuringLiveAnalysis>False</RunAnalyzersDuringLiveAnalysis>
		<NuGetAuditMode>direct</NuGetAuditMode>
	</PropertyGroup>

	<!-- Matches test projects but excludes non-test support libraries -->
	<PropertyGroup Condition="($(MSBuildProjectDirectory.Contains('$(MSBuildDirectorySeparatorChar)Tests$(MSBuildDirectorySeparatorChar)')) OR $(MSBuildProjectName.Contains('Test')) OR $(MSBuildProjectDirectory.Contains('ScratchPad'))) AND !$(MSBuildProjectName.Contains('.Testing'))">
		<IsTestProject>true</IsTestProject>
	</PropertyGroup>
	
	<Import Project="msbuild\IncludeTestConfigurationFiles.props" Condition="'$(IsTestProject)' == 'true'" />
	<Import Project="msbuild\IncludeTestAppSettings.props" Condition="'$(IsTestProject)' == 'true'" />
	<Import Project="msbuild\IncludeFluentAssertionsModuleInitializer.props" Condition="'$(IsTestProject)' == 'true'" />
</Project>