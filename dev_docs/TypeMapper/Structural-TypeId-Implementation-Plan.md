# Structural TypeId — Implementation Plan

> **No backward compatibility constraints.** There are zero deployed applications using this system. No persisted data exists. Any format, ID, or behavior can change freely.

Incremental build. Each phase is testable in isolation before moving to the next.

## Phase 1 — New types, testable in isolation ✅ COMPLETE

### 1.1 TypeId hierarchy + OpenGenericId ✅
- `StructuralTypeId` (abstract base) — `StringRepresentation` property, equality by string
- `MappedTypeId` — GUID + string `"GUID, 0"`. Equality by GUID (faster).
- `StableNameTypeId` — unmodified `AssemblyQualifiedName`
- `ConstructedTypeId` — mixed AQN with `GUID, 0` components
- `OpenGenericId` — GUID-backed struct, NOT a `StructuralTypeId`
- Files: `src/.../StructuralTypeId.cs`, `src/.../OpenGenericId.cs`

### 1.2 TypeNameParser ✅
Parses `AssemblyQualifiedName`-format strings into `ParsedTypeName` tree. Round-trips all formats.

- File: `src/.../Implementation/TypeNameParser.cs`
- Tests: `test/.../Refactoring/Naming/TypeNameParser_specification.cs`

### 1.3 TypeNameMapper ✅
Transforms `Type` ↔ `StructuralTypeId` using parser + mapping dictionaries. Both directions cached.

- File: `src/.../Implementation/TypeNameMapper.cs`
- Tests: `test/.../Refactoring/Naming/TypeNameMapper_specification.cs`

## Phase 2 — Registration API ✅ COMPLETE

### 2.1 Mapping declaration ✅
- `TypeMappingsAttribute`, `ITypeMappingDeclaration`, `ITypeMappingRegistrar`, `TypeMappingRegistrar`
- Files: `src/.../TypeMappingDeclaration.cs` (public API), `src/.../Implementation/TypeMappingRegistrar.cs`

### 2.2 Container registration ✅
- `TypeNameMapperBuilder` — fluent builder with auto-detection of Microsoft assemblies
- File: `src/.../Implementation/TypeNameMapperBuilder.cs`

## Phase 3 — Build standalone system, then swap

### 3.1 Revert grafting ✅
The initial 3.1 approach grafted new methods onto the old `ITypeMapper`/`TypeMapper`. Reverted to get clean separation.

### 3.2 Build new standalone system ✅
- **`IStructuralTypeMapper`** — new public interface: `GetId`/`GetType`/`TryGetType` (GUID-based), `GetIdForTypesAssignableTo`, `AssertMappingsExistFor`, `ToPersistedTypeString`/`FromPersistedTypeString`
- **`StructuralTypeMapper`** — implementation built from `[TypeMappings]` attributes via `TypeNameMapperBuilder`, with `BuildFromLoadedAssemblies()` factory + automatic incorporation of newly loaded assemblies
- Files: `src/.../IStructuralTypeMapper.cs`, `src/.../Implementation/StructuralTypeMapper.cs`
- Tests: `test/.../StructuralTypeMapper_specification.cs`

### 3.3 Migrate assemblies to new registration API ✅
All 13 assemblies have `[assembly: TypeMappings(...)]` + `ITypeMappingDeclaration` declarations (in `TypeMappingDeclarations.cs` files).

### 3.4 Swap old system for new ✅ COMPLETE
All consumers now use `IStructuralTypeMapper`. The old `.TypeMapper()` DI registration is not called anywhere.

DI registration sites (all use `.StructuralTypeMapper()`):
- `src/Compze.Hosting/ServerEndpointBuilder.cs`
- `src/Compze.Tessaging.Hosting.Testing/Wiring/DiContainerExtensions.cs`
- `src/Compze.Tessaging.Hosting.Testing/Tessaging/TestClient.cs`
- `test/Compze.Tests.Performance.Internals/.../TeventStoreTeventSerializerPerformanceTests.cs`
- `test/Compze.Internals.Serialization.Newtonsoft.Specifications/SerializerTest.cs`

Active consumers of `IStructuralTypeMapper`:
- `DocumentDb`, `TeventStore`, `TessagingController`, `TypermediaController`
- `TaggregateTypeValidator`, `InfrastructureQueryController`, `TypermediaRouter`
- `RenamingDecorator` (Newtonsoft serialization)

### 3.5 Remove old infrastructure ✅ COMPLETE

All old system files deleted. Zero references remain:
- `ITypeMapper.cs`, `TypeMapper.cs`, `TypeId.cs`
- `AssemblyMappingReader.cs`, `MissingMappingReporter.cs`, `TypeMapperSourceCodeGenerator.cs`, `DeterministicTypeId.cs`
- 13 `AutoGeneratedCurrentAssemblyTypeMapperForRefactoringSupport.cs` files

**Kept** (still used by `StructuralTypeMapper` for constructed-type discovery):
- `TypeMapperAssemblyScanner.cs`
- `TypeMapperTypes.cs`

### 3.6 Verify full test suite ✅
2303 passed, 0 failed, 26 skipped (expected skips).
